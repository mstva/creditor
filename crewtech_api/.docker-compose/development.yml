version: '3.9'

services:

  django:
    image: crewtech_development
    container_name: crewtech_development_django
    build:
      context: ..
      dockerfile: Dockerfile
    restart: always
    env_file:
      - ../.env/.env.development
    ports:
      - "80:8000"
    volumes:
      - ../:/crewtech_api
    command: sh -c "chmod +x /crewtech_api/entrypoint.sh && sh /crewtech_api/entrypoint.sh"

  celery:
    image: crewtech_development
    container_name: crewtech_development_celery
    build:
      context: ..
      dockerfile: Dockerfile
    restart: always
    command: /opt/venv/bin/celery -A src worker -l INFO
    env_file:
      - ../.env/.env.development
    volumes:
      - ../:/crewtech_api
    depends_on:
      - django
